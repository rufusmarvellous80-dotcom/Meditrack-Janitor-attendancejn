# Meditrack (Firebase-Enabled starter)
This project contains a Next.js 13 app router skeleton with Firebase client setup included.
Replace or expand the pages/components as needed. To run locally:

1. npm install
2. npm run dev
3. Open http://localhost:3000

The firebase config was inserted into lib/firebase.ts from the data you provided.
